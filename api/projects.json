---
layout: null
---
{
  {% for project in site.projects %}
    "{{ project.url | remove: '.html' }}": {
      "title": {{ project.title | jsonify }},
      "content": {{ project.content | strip_newlines | jsonify }}
    }
    {% unless forloop.last %},{% endunless %}
  {% endfor %}
}